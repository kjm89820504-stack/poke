# Character Image

Save the provided character image as `pikachu.png` in this folder:

```text
assets/pikachu.png
```

A transparent PNG looks best. If the image has a black background, the game removes near-black pixels at runtime so it can blend into the canvas.
